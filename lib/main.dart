import 'package:flutter/material.dart';
import './route/route.dart';

main() => runApp(DBApplication());

class DBApplication extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return RouteClass();
  }
}
